// 9. Write a js program to insert an element in an array.

let arr = [20, 30, 15, 17, 45, 50, 2, 5, 100, 103];

let element = 1000;

console.log(`Before inserting element to array:\n${arr}`);

arr.push(element);

console.log(`\nAfter inserting element to array:\n${arr}`);