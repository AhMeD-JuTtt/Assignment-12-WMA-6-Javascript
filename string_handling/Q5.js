// 5. Write a js program to convert lowercase string to uppercase.

let str = "my name is muhammad ahmed.";

// Before converting to uppercase:

console.log(`Before converting to uppercase:\n"${str}"`)

// After converting to uppercase:

str = str.toUpperCase();

console.log(`\nAfter converting to uppercase:\n"${str}"`)