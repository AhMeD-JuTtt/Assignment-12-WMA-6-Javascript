// 6. Write a js program to convert uppercase string to lowercase.

let str = "MY NAME IS MUHAMMAD AHMED.";

// Before converting to lowercase:

console.log(`Before converting to lowercase:\n"${str}"`)

// After converting to uppercase:

str = str.toLowerCase();

console.log(`\nAfter converting to lowercase:\n"${str}"`)